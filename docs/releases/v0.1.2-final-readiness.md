# v0.1.2 final readiness assessment

Assessment date: September 13, 2026. This document records release readiness;
it is not a release announcement. See the [gate report](v0.1.2-gate.md) for commands,
source records, versions, and validation scope.

## VERIFIED

| Area | Evidence |
|---|---|
| Python | 146 tests passed; 78% combined statement/branch coverage |
| Focused checks | 136 non-browser, 10 browser, 33 targeted agent/runner, and 16 release-gate tests passed; overlapping subsets |
| Deterministic evaluation | 16/16 authored cases in both variants; no inference credentials required |
| Static checks | Ruff lint/format and Mypy across 14 source files passed |
| Frontend | TypeScript/Vite production build and formatting passed |
| Dependency checks | npm audit: zero vulnerabilities; Python audit: zero known vulnerabilities in checked installed dependencies |
| Hosted checks | Required PR checks passed; CodeQL results reviewed with zero findings in the checked analysis |
| Unfamiliar authored incident | Actual GitHub ingestion and live agent planning; Linux baseline 0/3, intervention 3/3, deterministic `supported` |
| Replay | Three replays of `inv-bc1e08adab69`; zero new model calls or Actions jobs; case, evidence, and artifact hashes unchanged |
| Security and recovery | Authentication, deduplication, credential stripping, action/artifact bounds, budgets, citations, checkpoint identity/report/corruption checks exercised |
| Publication boundaries | Runtime state and secrets excluded; original evidence preserved; PR remains a draft; existing tags unchanged |

The [measurement record](../validation/release-gate-results.json) preserves both
incomplete live attempts separately from successful experiments. These checks cover
authored development validation and specific recorded environments.

## BLOCKED

**Live provider release gate — HTTP 402/payment availability**

The latest run made seven inference attempts: six succeeded and one failed before
the unknown fixture's diagnosis. Successful calls reported 8,723 input and 704 output
tokens; cost is unavailable. Overlay, selector, and API-contract fixtures passed.
The earlier run stopped during API-contract planning. Neither attempt completes the
frozen four-fixture gate. Provider errors receive no diagnostic or abstention credit.

The configured model remains `Qwen/Qwen3-235B-A22B-Instruct-2507:novita`, with
`failurelab-agents-v3` and `reviewed-interventions-v1`. The exact provider-side billing
decision and account balance are unavailable. No further provider requests were made
for the final documentation pass.

## NOT VERIFIED

**Deployment smoke test — local Docker/PostgreSQL unavailable**

Container startup and PostgreSQL-backed persistence/recovery need their actual
deployment prerequisites. The existing disposable Linux invoice experiment does not
verify those deployment paths. Public GitHub webhook delivery and a permanent private
runner endpoint also remain unverified. Independent model accuracy, production
capacity, and developer time savings have not been measured.

## PENDING

**Human review**

The original incident has no recorded human review. PR #14 remains open and draft;
its passing checks do not replace review of the evidence and release preconditions.

### Release decision

**DO NOT RELEASE v0.1.2**

No `v0.1.2` tag or GitHub release has been created. Prepared package version metadata
does not authorize publication.

### Release preconditions

1. Have the account owner restore provider inference/payment availability without
   substituting the model or changing the frozen prompt, fixtures, or scorer.
2. Run the command below from the reviewed checkout with the existing local provider
   configuration. It makes at most eight model calls in a fresh workspace and stops
   on failure. Retain failed attempts and source/version provenance.

   ```powershell
   uv run python scripts/verify_live_agents.py
   ```

3. Require a complete passing result for all four fixtures, including the unknown
   case's expected abstention. Do not combine partial runs or count a provider error
   as a completed case.
4. With the actual prerequisites available, follow the [deployment procedure](../deployment.md)
   and verify container health, browser startup, PostgreSQL persistence/checkpoints,
   and recovery. Preserve measured smoke outputs and the tested environment.
5. Obtain human review of the incident and PR, check the final revision's required
   GitHub checks and scanner results, and resolve any resulting findings.
6. Obtain explicit authorization before merging through the protected PR process and
   creating the `v0.1.2` tag/release. Do not move existing tags or rewrite history.

To check the retained completed invoice case locally, with its original ignored
workspace present:

```powershell
uv run python scripts/verify_unfamiliar_incident.py run --attempt 3
```

That command reuses the completed checkpoint; it is not a new live acceptance run
and does not record human review. A fresh clone does not include the local checkpoint
or raw traces; the [incident reproduction instructions](../unfamiliar-ci-validation.md#reproduction)
describe creating a separate new investigation when those records are unavailable.
