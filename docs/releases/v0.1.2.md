# FailureLab v0.1.2 preparation

Version metadata is prepared for `0.1.2`. No release tag has been created.

The [single-incident validation](../unfamiliar-ci-validation.md) records an authored
GitHub CI failure, actual ingestion, live agent calls, and controlled Chromium runs
on a disposable Linux VM. It includes two inconclusive attempts and the successful
attempt after correcting the tool descriptions.

Repository experiments now retain bounded, integrity-checked PNG/ZIP artifacts.
The planning schema explains intervention effects and baseline repetition limits.
Prompt version `failurelab-agents-v3` records that change. Tests cover artifact
boundaries and delivery of generic tool descriptions to the provider.

The local suite passed 146 tests. The frontend build, lint/format/type checks, npm audit,
and Python dependency audit passed. The new incident completed with baseline 0/3
and intervention 3/3. The updated four-fixture live check remains incomplete after
a provider-access failure. Complete the live acceptance, deployment smoke checks,
and human review before publication authorization.

The [September 13 release check](v0.1.2-gate.md) records the two incomplete live
reruns, verified replay, provider failure classification, provenance changes, and
remaining infrastructure limits. Release status is `BLOCKED`.

The [final readiness assessment](v0.1.2-final-readiness.md) lists the release
preconditions. PR #14 remains a draft; no tag or release is authorized by these
version-preparation notes.

No independent diagnostic accuracy, production capacity, or developer time savings
claim is made. The validation record contains exact commands, measurements, source
links, and remaining deployment limitations.
